-- ✅ Correct SQL (fully qualified with schema)
CREATE SCHEMA IF NOT EXISTS og;

CREATE TABLE og.customers (
    id INT PRIMARY KEY,
    name STRING,
    created_at TIMESTAMP
);

-- ❌ Wrong SQL: typo in TABLE keyword (will fail in syntax check mode)
CREATE TABL og.orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    total NUMBER
);

-- ✅ Correct SQL
CREATE SCHEMA IF NOT EXISTS bronze;

CREATE TABLE bronze.products (
    product_id INT PRIMARY KEY,
    product_name STRING,
    price NUMBER(10,2)
);

-- ❌ Wrong SQL: extra comma before closing parenthesis
CREATE SCHEMA IF NOT EXISTS silver;

CREATE TABLE silver.payments (
    payment_id INT,
    amount NUMBER,
);

-- ❌ Wrong SQL: invalid datatype STRIN instead of STRING
CREATE SCHEMA IF NOT EXISTS gold;

CREATE TABLE gold.suppliers (
    supplier_id INT,
    supplier_name STRIN
);

-- ✅ Correct SQL
CREATE TABLE og.inventory (
    item_id INT PRIMARY KEY,
    stock_quantity INT,
    last_updated DATE
);
