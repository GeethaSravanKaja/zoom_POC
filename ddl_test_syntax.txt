-- ✅ Correct SQL
CREATE SCHEMA IF NOT EXISTS bronze;

-- ✅ Correct SQL
CREATE TABLE IF NOT EXISTS bronze.customers (
    customer_id INT,
    customer_name STRING,
    email STRING
);

-- ✅ Correct SQL
CREATE TABLE IF NOT EXISTS bronze.orders (
    order_id INT,
    customer_id INT,
    amount NUMBER(10,2),
    order_date DATE
);

-- ❌ Wrong SQL: typo in CREATE keyword
CRETAE TABLE bronze.bad_table (
    id INT,
    value STRING
);

-- ❌ Wrong SQL: invalid datatype "STRIN"
CREATE TABLE gold.suppliers (
    supplier_id INT,
    supplier_name STRIN
);

-- ❌ Wrong SQL: extra comma before closing parenthesis
CREATE TABLE silver.payments (
    payment_id INT,
    order_id INT,
    amount NUMBER(10,2),
);
